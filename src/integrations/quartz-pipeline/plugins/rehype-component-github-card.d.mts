/** Loose declaration for the hand-written rehype component (plain JS module,
 * allowJs: false). Renders :::github cards from directive nodes. */
export function GithubCardComponent(props: unknown, children: unknown): unknown;
