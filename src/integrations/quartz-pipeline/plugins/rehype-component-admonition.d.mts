/** Loose declaration for the hand-written rehype component (plain JS module,
 * allowJs: false). Renders :::note/tip/... callouts from directive nodes. */
export function AdmonitionComponent(
	props: unknown,
	children: unknown,
	title: string,
): unknown;
